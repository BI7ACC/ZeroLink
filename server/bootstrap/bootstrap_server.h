#ifndef ZEROLINK_BOOTSTRAP_SERVER_H
#define ZEROLINK_BOOTSTRAP_SERVER_H

/**
 * @brief 启动引导服务器并开始监听。
 * @param port 要监听的端口号。
 * @return 成功返回 0，失败返回非 0。
 */
int start_bootstrap_server(int port);

#endif //ZEROLINK_BOOTSTRAP_SERVER_H
